const API = "http://localhost:8000";

export const urls = {
  auth: `${API}/auth`,
  users: `${API}/users`,
  characters: `${API}/characters`,
  campaigns: `${API}/campaigns`,
  games: `${API}/games`,
}
